apt install wget -y
apt install squid -y
wget https://raw.githubusercontent.com/denoxzipper/denoxzipper/main/tinmaung
rm -rf /etc/squid/squid.conf
mv squid.conf /etc/squid/squid.conf
service squid restart
netstat -tulpn 
